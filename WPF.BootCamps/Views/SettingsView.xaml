<Window x:Class="WPF.BootCamps.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.BootCamps.Views"
        mc:Ignorable="d"
        Title="SettingsView" 
        Height="550" 
        Width="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="White"
        FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary Source="/Themes/SettingsStyles.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="{StaticResource SidebarBack}" BorderBrush="#E2E8F0" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20">
                <TextBlock Text="SETTINGS" FontSize="12" FontWeight="Bold" Foreground="#94A3B8" Margin="20,0,0,20"/>

                <ListBox x:Name="lstSettingsMenu" Background="Transparent" BorderThickness="0" SelectedIndex="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="bd" Padding="20,12" Background="Transparent" BorderBrush="{StaticResource AccentBlue}" BorderThickness="0">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#EFF6FF"/>
                                                <Setter TargetName="bd" Property="BorderThickness" Value="4,0,0,0"/>
                                                <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <TextBlock Text="Appearance" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="Language &amp; Region" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="General &amp; Updates" FontSize="14" FontWeight="SemiBold"/>
                </ListBox>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="30,20">
                <StackPanel>
                    <TextBlock Text="Appearance Settings" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,0,0,25"/>

                    <GroupBox Header="Theme Mode">
                        <UniformGrid Columns="3" Margin="0,5">
                            <RadioButton Content="Light" IsChecked="True" Style="{StaticResource {x:Type RadioButton}}"/>
                            <RadioButton Content="Dark" />
                            <RadioButton Content="System" />
                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Header="Brand Color">
                        <StackPanel>
                            <ComboBox SelectedIndex="0" Height="35">
                                <ComboBoxItem Content="Ocean Blue"/>
                                <ComboBoxItem Content="Forest Green"/>
                                <ComboBoxItem Content="Ruby Red"/>
                            </ComboBox>
                            <CheckBox Content="Use system accent colors" Margin="0,15,0,0"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Typography">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Font Family" FontSize="11" Foreground="#64748B" Margin="0,0,0,5"/>
                                <ComboBox SelectedIndex="0">
                                    <ComboBoxItem Content="Segoe UI"/>
                                    <ComboBoxItem Content="Roboto"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Font Size" FontSize="11" Foreground="#64748B" Margin="0,0,0,5"/>
                                <Slider Minimum="10" Maximum="20" Value="14"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="1" Padding="20" Background="White" BorderBrush="#F1F5F9" BorderThickness="0,1,0,0">
                <Grid>
                    <Button Content="Reset to Default" HorizontalAlignment="Left" Foreground="#64748B" Background="Transparent" BorderThickness="0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource ActionButton}" Background="#F1F5F9" Foreground="#475569" Click="BtnCancel_Click"/>
                        <Button Content="Save Changes" Style="{StaticResource ActionButton}" Background="{StaticResource AccentBlue}" Foreground="White" Click="BtnSave_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
    
</Window>
