<Window x:Class="WPF.BootCamps.Views.Day_6.ColorSystem"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.BootCamps.Views.Day_6"
        mc:Ignorable="d"
        Title="ColorSystem" Height="450" Width="800">

    <Window.Resources>
        <SolidColorBrush
            x:Key="PrimaryBrush"
            Color="#2962FF"/>
        <!--  للازرار الاساسية و اللرئيسية -->
        <SolidColorBrush
            x:Key="SecondaryBrush"
            Color="#455A64"/>
        <!-- للازرار الثانوية -->
        <SolidColorBrush
            x:Key="SurfaceBrush"
            Color="White"/>
        <!-- للكروت -->
        <SolidColorBrush
            x:Key="BackgroundBrush"
            Color="#F0F2F5"/>
        <!-- للخلفيات -->
        <SolidColorBrush
            x:Key="TextPrimaryBrush"
            Color="#212121"/>
        <!-- للنصوص الاساسية -->
        <SolidColorBrush
            x:Key="TextSecondaryBrush"
            Color="#757575"/>
        <!-- للنصوص الثانوية-->
        <!--  هنا صار عدنا نظام الالوان كامل -->
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <TextBlock
            Text="Welcome To WPF Color System"
            FontSize="18" FontWeight="Bold"
            Grid.Row="0"
            HorizontalAlignment="Center"
            Margin="0,10,0,0"/>
        <Button
            Content="Save"
            Grid.Row="3" Grid.Column="1"
            Background="{StaticResource PrimaryBrush}"
            Foreground="{StaticResource TextPrimaryBrush}"
            FontSize="18" FontWeight="Bold" Margin="316,291,316,83"/>
    </Grid>

</Window>
